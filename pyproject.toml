[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
  ".oscanner",
  ".pytest_cache",
  "__pycache__",
  "frontend",
  "logs",
]

[tool.ruff.lint]
select = ["E9", "F"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
